apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: ecommerce
  labels:
    app: identity-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
        - name: identity-service
          image: ecommerce/identity-service:0.0.1
          imagePullPolicy: IfNotPresent
          env:
            # Debe coincidir con el Secret usado por api-gateway.
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecommerce-jwt
                  key: secret
            - name: SPRING_PROFILES_ACTIVE
              value: postgres
            - name: POSTGRES_URL
              value: jdbc:postgresql://identity-postgres:5432/identity_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: identity-postgres
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: identity-postgres
                  key: password
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: ecommerce
  labels:
    app: identity-service
spec:
  type: ClusterIP
  selector:
    app: identity-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
